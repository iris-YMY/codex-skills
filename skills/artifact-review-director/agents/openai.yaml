interface:
  display_name: "Artifact Review Director"
  short_description: "Run QA and user review before final artifact delivery"
  default_prompt: "Use $artifact-review-director to QA this artifact, show an appropriate preview, and wait for approval before final delivery."
policy:
  allow_implicit_invocation: true
